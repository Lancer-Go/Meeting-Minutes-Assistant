# Meeting Minutes Assistant 依赖（Python 3.11，锁定版本）

# ---- 通用 ----
python-dotenv==1.2.3

# ---- 音频（FFmpeg）----
# 系统 FFmpeg 优先；缺失时用 static-ffmpeg 联网下载兜底（也可手动放置到 tools/ffmpeg/bin/）
static_ffmpeg==3.0

# ---- 语音转写 (ASR) ----
faster-whisper==1.2.1           # 本地离线转写（离线兜底，无需密钥）
alibabacloud-nls-python-sdk==1.0.2  # 阿里云 NLS 实时语音转写（可选）
aliyun-python-sdk-core==2.16.0  # 阿里云 NLS 录音文件识别（可选）
oss2==2.19.1                    # 阿里云 OSS 上传（NLS 录音文件识别需音频 URL）
dashscope==1.27.1               # 阿里云 DashScope（备选）
tencentcloud-sdk-python-asr==3.1.160  # 腾讯云录音文件识别（主选）
websocket-client==1.9.0         # 讯飞（占位，未接入 LFASR）

# ---- 纪要生成 (LLM) ----
openai==3.3.1                   # DeepSeek / Qwen 的 OpenAI 兼容接口

# ---- 结构化 / 模板渲染 (M2) ----
jinja2==3.1.6                   # 纪要三套模板渲染

# ---- 样例数据生成 ----
edge-tts==7.2.8                 # 合成中文会议样例（真实音频由用户替换）

# ---- 服务 (M1) ----
fastapi==0.141.1
uvicorn[standard]==0.52.4
python-multipart==0.0.32

# ---- 测试 (M1) ----
pytest==9.1.1
httpx==0.28.1

# ---- M3 · 生产存储（TG-2）----
sqlalchemy==2.0.52              # ORM 双模式（sqlite:// 本地 / postgresql:// 生产）
psycopg2-binary==2.9.12         # PostgreSQL 驱动
boto3==1.43.80                  # MinIO / S3 兼容对象存储

# ---- M3 · 任务队列（TG-0）----
celery==5.6.3                   # 生产任务队列（替换 BackgroundTasks）
redis==8.1.0                    # Celery broker / backend

# ---- M3 · 可观测（TG-3）----
prometheus-client==0.26.0       # /metrics 指标端点

# ---- M3 · 安全（TG-4）----
PyJWT==2.13.0                   # JWT 签发 / 校验
bcrypt==5.0.0                   # 密码哈希
cryptography==50.0.0            # AES-256-GCM 应用层加密

# ---- M3 · 性能测试（TG-5）----
locust==2.46.4                  # 压测

# ---- M3 · 质量（TG-1）----
ruff==0.16.4                    # lint
pytest-cov==7.1.0               # 覆盖率
